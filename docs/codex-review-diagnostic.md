# Codex review connectivity diagnostic

This temporary document provides a minimal diff for a pull request whose head
and base branches both belong to `xLLM-AI/xllm`.

The diagnostic compares automatic review on PR creation and an explicit manual
review request with the unresponsive fork-based PR #2334.

The change affects documentation only and is not intended for release.
